import 'package:flutter/material.dart';

class BriefcaseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Briefcase 화면', style: TextStyle(fontSize: 24)),
    );
  }
}
